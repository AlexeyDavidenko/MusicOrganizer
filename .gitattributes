# Force LF line endings on checkout regardless of platform/local git config, so `dotnet format`
# (which enforces LF via .editorconfig) behaves identically on Linux/macOS/Windows CI runners.
* text=auto eol=lf
